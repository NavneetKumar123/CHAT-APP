var zlib = require('zlib');
 var fs = require('fs');

// var gzip = zlib.createGzip();
// var r = fs.createReadStream('demo.txt',);
// var w = fs.createWriteStream('demo.txt.gz');
// r.pipe(gzip).pipe(w);
// var zlib = require('zlib');
// var fs = require('fs');

// var gunzip = zlib.createGunzip();
// var r = fs.createReadStream('demo.txt.gz');
// var w = fs.createWriteStream('demo.txt');
// r.pipe(gunzip).pipe(w);


// var gzip=zlib.createGzip();
// var r=fs.createReadStream('student.txt')
// var w=fs.createWriteStream('student.txt.gz')
// r.pipe(gzip).pipe(w);


// var gunzip=zlib.createGunzip();
// var r= fs.createReadStream('student.txt.gz')
// var w= fs.createWriteStream('student.txt');
// r.pipe(gunzip).pipe(w)
